/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { PANDA_MONOPOLY_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const RESET_GAME_HANDLER_DISCRIMINATOR = new Uint8Array([
  205, 29, 157, 88, 253, 101, 84, 100,
]);

export function getResetGameHandlerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RESET_GAME_HANDLER_DISCRIMINATOR
  );
}

export type ResetGameHandlerInstruction<
  TProgram extends string = typeof PANDA_MONOPOLY_PROGRAM_ADDRESS,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountClock extends
    | string
    | AccountMeta<string> = 'SysvarC1ock11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountClock extends string
        ? ReadonlyAccount<TAccountClock>
        : TAccountClock,
      ...TRemainingAccounts,
    ]
  >;

export type ResetGameHandlerInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ResetGameHandlerInstructionDataArgs = {};

export function getResetGameHandlerInstructionDataEncoder(): FixedSizeEncoder<ResetGameHandlerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RESET_GAME_HANDLER_DISCRIMINATOR })
  );
}

export function getResetGameHandlerInstructionDataDecoder(): FixedSizeDecoder<ResetGameHandlerInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getResetGameHandlerInstructionDataCodec(): FixedSizeCodec<
  ResetGameHandlerInstructionDataArgs,
  ResetGameHandlerInstructionData
> {
  return combineCodec(
    getResetGameHandlerInstructionDataEncoder(),
    getResetGameHandlerInstructionDataDecoder()
  );
}

export type ResetGameHandlerInput<
  TAccountGame extends string = string,
  TAccountAuthority extends string = string,
  TAccountClock extends string = string,
> = {
  game: Address<TAccountGame>;
  authority: TransactionSigner<TAccountAuthority>;
  clock?: Address<TAccountClock>;
};

export function getResetGameHandlerInstruction<
  TAccountGame extends string,
  TAccountAuthority extends string,
  TAccountClock extends string,
  TProgramAddress extends Address = typeof PANDA_MONOPOLY_PROGRAM_ADDRESS,
>(
  input: ResetGameHandlerInput<TAccountGame, TAccountAuthority, TAccountClock>,
  config?: { programAddress?: TProgramAddress }
): ResetGameHandlerInstruction<
  TProgramAddress,
  TAccountGame,
  TAccountAuthority,
  TAccountClock
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PANDA_MONOPOLY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    game: { value: input.game ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    clock: { value: input.clock ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.clock.value) {
    accounts.clock.value =
      'SysvarC1ock11111111111111111111111111111111' as Address<'SysvarC1ock11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.clock),
    ],
    data: getResetGameHandlerInstructionDataEncoder().encode({}),
    programAddress,
  } as ResetGameHandlerInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountAuthority,
    TAccountClock
  >);
}

export type ParsedResetGameHandlerInstruction<
  TProgram extends string = typeof PANDA_MONOPOLY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    game: TAccountMetas[0];
    authority: TAccountMetas[1];
    clock: TAccountMetas[2];
  };
  data: ResetGameHandlerInstructionData;
};

export function parseResetGameHandlerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedResetGameHandlerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      game: getNextAccount(),
      authority: getNextAccount(),
      clock: getNextAccount(),
    },
    data: getResetGameHandlerInstructionDataDecoder().decode(instruction.data),
  };
}
