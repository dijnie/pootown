/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { PANDA_MONOPOLY_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const PAY_MEV_TAX_HANDLER_DISCRIMINATOR = new Uint8Array([
  72, 9, 102, 128, 113, 199, 253, 113,
]);

export function getPayMevTaxHandlerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PAY_MEV_TAX_HANDLER_DISCRIMINATOR
  );
}

export type PayMevTaxHandlerInstruction<
  TProgram extends string = typeof PANDA_MONOPOLY_PROGRAM_ADDRESS,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountPlayerState extends string | AccountMeta<string> = string,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountClock extends
    | string
    | AccountMeta<string> = 'SysvarC1ock11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountPlayerState extends string
        ? WritableAccount<TAccountPlayerState>
        : TAccountPlayerState,
      TAccountPlayer extends string
        ? WritableSignerAccount<TAccountPlayer> &
            AccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountClock extends string
        ? ReadonlyAccount<TAccountClock>
        : TAccountClock,
      ...TRemainingAccounts,
    ]
  >;

export type PayMevTaxHandlerInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type PayMevTaxHandlerInstructionDataArgs = {};

export function getPayMevTaxHandlerInstructionDataEncoder(): FixedSizeEncoder<PayMevTaxHandlerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PAY_MEV_TAX_HANDLER_DISCRIMINATOR })
  );
}

export function getPayMevTaxHandlerInstructionDataDecoder(): FixedSizeDecoder<PayMevTaxHandlerInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getPayMevTaxHandlerInstructionDataCodec(): FixedSizeCodec<
  PayMevTaxHandlerInstructionDataArgs,
  PayMevTaxHandlerInstructionData
> {
  return combineCodec(
    getPayMevTaxHandlerInstructionDataEncoder(),
    getPayMevTaxHandlerInstructionDataDecoder()
  );
}

export type PayMevTaxHandlerAsyncInput<
  TAccountGame extends string = string,
  TAccountPlayerState extends string = string,
  TAccountPlayer extends string = string,
  TAccountClock extends string = string,
> = {
  game: Address<TAccountGame>;
  playerState?: Address<TAccountPlayerState>;
  player: TransactionSigner<TAccountPlayer>;
  clock?: Address<TAccountClock>;
};

export async function getPayMevTaxHandlerInstructionAsync<
  TAccountGame extends string,
  TAccountPlayerState extends string,
  TAccountPlayer extends string,
  TAccountClock extends string,
  TProgramAddress extends Address = typeof PANDA_MONOPOLY_PROGRAM_ADDRESS,
>(
  input: PayMevTaxHandlerAsyncInput<
    TAccountGame,
    TAccountPlayerState,
    TAccountPlayer,
    TAccountClock
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  PayMevTaxHandlerInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountPlayerState,
    TAccountPlayer,
    TAccountClock
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PANDA_MONOPOLY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    game: { value: input.game ?? null, isWritable: true },
    playerState: { value: input.playerState ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: true },
    clock: { value: input.clock ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.playerState.value) {
    accounts.playerState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([112, 108, 97, 121, 101, 114])),
        getAddressEncoder().encode(expectAddress(accounts.game.value)),
        getAddressEncoder().encode(expectAddress(accounts.player.value)),
      ],
    });
  }
  if (!accounts.clock.value) {
    accounts.clock.value =
      'SysvarC1ock11111111111111111111111111111111' as Address<'SysvarC1ock11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.playerState),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.clock),
    ],
    data: getPayMevTaxHandlerInstructionDataEncoder().encode({}),
    programAddress,
  } as PayMevTaxHandlerInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountPlayerState,
    TAccountPlayer,
    TAccountClock
  >);
}

export type PayMevTaxHandlerInput<
  TAccountGame extends string = string,
  TAccountPlayerState extends string = string,
  TAccountPlayer extends string = string,
  TAccountClock extends string = string,
> = {
  game: Address<TAccountGame>;
  playerState: Address<TAccountPlayerState>;
  player: TransactionSigner<TAccountPlayer>;
  clock?: Address<TAccountClock>;
};

export function getPayMevTaxHandlerInstruction<
  TAccountGame extends string,
  TAccountPlayerState extends string,
  TAccountPlayer extends string,
  TAccountClock extends string,
  TProgramAddress extends Address = typeof PANDA_MONOPOLY_PROGRAM_ADDRESS,
>(
  input: PayMevTaxHandlerInput<
    TAccountGame,
    TAccountPlayerState,
    TAccountPlayer,
    TAccountClock
  >,
  config?: { programAddress?: TProgramAddress }
): PayMevTaxHandlerInstruction<
  TProgramAddress,
  TAccountGame,
  TAccountPlayerState,
  TAccountPlayer,
  TAccountClock
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PANDA_MONOPOLY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    game: { value: input.game ?? null, isWritable: true },
    playerState: { value: input.playerState ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: true },
    clock: { value: input.clock ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.clock.value) {
    accounts.clock.value =
      'SysvarC1ock11111111111111111111111111111111' as Address<'SysvarC1ock11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.playerState),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.clock),
    ],
    data: getPayMevTaxHandlerInstructionDataEncoder().encode({}),
    programAddress,
  } as PayMevTaxHandlerInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountPlayerState,
    TAccountPlayer,
    TAccountClock
  >);
}

export type ParsedPayMevTaxHandlerInstruction<
  TProgram extends string = typeof PANDA_MONOPOLY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    game: TAccountMetas[0];
    playerState: TAccountMetas[1];
    player: TAccountMetas[2];
    clock: TAccountMetas[3];
  };
  data: PayMevTaxHandlerInstructionData;
};

export function parsePayMevTaxHandlerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedPayMevTaxHandlerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      game: getNextAccount(),
      playerState: getNextAccount(),
      player: getNextAccount(),
      clock: getNextAccount(),
    },
    data: getPayMevTaxHandlerInstructionDataDecoder().decode(instruction.data),
  };
}
